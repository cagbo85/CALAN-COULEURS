import React from "react";
import ReactDOM from "react-dom/client";
import ProgrammationSection from "./components/ProgrammationSection";

ReactDOM.createRoot(
    document.getElementById("programmation-root"),
).render(
    <React.StrictMode>
        <ProgrammationSection />
    </React.StrictMode>,
);
